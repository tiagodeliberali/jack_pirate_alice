class Main {
    function void main() {
        var Game game;
        var int level;

        let level = StartScreen.getLevel();

            let game = Game.new(level);

            do game.run();
            do game.dispose();

            do WinScreen.showMessage();

        return;
    }
}